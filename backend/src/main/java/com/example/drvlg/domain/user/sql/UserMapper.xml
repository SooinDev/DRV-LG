<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.drvlg.domain.user.mapper.UserMapper">

  <insert id="insertUser">
    INSERT INTO USER (
      email, password, nickname, created_at, updated_at
    ) VALUES (
               #{email}, #{password}, #{nickName}, NOW(), NOW()
             )
  </insert>

  <select id="selectUserByEmail" resultType="com.example.drvlg.domain.user.vo.UserVO">
    SELECT
      user_id,
      email,
      password,
      nickname,
      created_at,
      updated_at,
      refresh_token,
      refresh_token_expires_at
    FROM
      USER
    WHERE
      email = #{email}
  </select>

  <update id="updateRefreshToken">
    UPDATE USER
    SET
      refresh_token = #{refreshToken},
      refresh_token_expires_at = #{refreshTokenExpiresAt}
    WHERE
      user_id = #{userId}
  </update>

</mapper>